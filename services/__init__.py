# services — application service layer.
#
# Contains orchestration logic that coordinates domain modules into coherent
# use-case pipelines.  No FastAPI, no Streamlit, no HTTP concepts.
# Both the API layer (api/) and the UI layer (ui/) import from here.
